[background]
path = "/usr/share/backgrounds/lock_bg.png"
fit = "Cover"

[GTK]
application_prefer_dark_theme = true

[commands]
reboot = [ "systemctl", "reboot" ]
poweroff = [ "systemctl", "poweroff" ]
